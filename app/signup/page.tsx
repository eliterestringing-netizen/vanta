import Link from "next/link";import { SiteHeader } from "@/components/site-header";
export default function Signup(){return <><SiteHeader/><main className="page"><section className="card" style={{maxWidth:430,margin:"auto"}}><h1>Create your account</h1><p className="muted">Your account will hold every domain you manage with Lucky Domains.</p>{["Full name","Email address","Password"].map((x,i)=><input key={x} type={i===2?"password":"text"} placeholder={x} aria-label={x} style={{padding:12,border:"1px solid var(--line)",borderRadius:8,width:"100%",margin:"8px 0"}}/>)}<Link className="button" href="/dashboard" style={{width:"100%",marginTop:10}}>Create account</Link><p className="muted">Already have an account? <Link href="/login">Log in</Link></p></section></main></>}
