"use client";
import { DashboardLayout } from "@/components/dashboard-nav";import {useState} from "react";
export default function Renew(){const [years,setYears]=useState(1);return <DashboardLayout><h1>Renew domain</h1><p className="muted">Keep example.com.au yours for longer.</p><section className="card" style={{maxWidth:640,marginTop:24}}><div className="grid3"><div><div className="statlabel">Current expiry</div><h3>14 Sep 2027</h3></div><div><div className="statlabel">New expiry</div><h3>14 Sep {2027+years}</h3></div><div><div className="statlabel">Renewal price</div><h3>${(24.95*years).toFixed(2)}</h3></div></div><label>Renewal period <select value={years} onChange={e=>setYears(+e.target.value)} style={{marginLeft:10}}>{[1,2,3,5].map(x=><option key={x}>{x}</option>)}</select> years</label><p><input type="checkbox" defaultChecked/> Keep auto-renew enabled</p><button className="button">Continue to mock checkout</button></section></DashboardLayout>}
