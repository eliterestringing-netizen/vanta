"use client";
import { DashboardLayout } from "@/components/dashboard-nav";import {useState} from "react";
export default function Nameservers(){const [editing,setEditing]=useState(false);const [saved,setSaved]=useState(false);return <DashboardLayout><h1>Nameservers</h1><p className="muted">Choose who controls DNS for this domain.</p><section className="card" style={{maxWidth:700,marginTop:24}}><h3>Current nameservers</h3><p>ns1.vanta.example</p><p>ns2.vanta.example</p>{editing&&<><div className="notice">Changing nameservers may interrupt website or email service until new DNS records are configured.</div><input defaultValue="ns1.vanta.example" aria-label="Primary nameserver" style={{width:"100%",padding:10,marginTop:12}}/><input defaultValue="ns2.vanta.example" aria-label="Secondary nameserver" style={{width:"100%",padding:10,marginTop:8}}/></>}{saved&&<p className="available">Changes saved in development mode.</p>}<button className="button" onClick={()=>editing?setSaved(true):setEditing(true)} style={{marginTop:16}}>{editing?"Confirm changes":"Edit nameservers"}</button></section></DashboardLayout>}
