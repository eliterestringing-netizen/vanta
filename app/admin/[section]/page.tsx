import { DashboardLayout } from "@/components/dashboard-nav";
export default async function AdminSection({params}:{params:Promise<{section:string}>}){const {section}=await params;const name=section.replace(/-/g," ").replace(/\b\w/g,x=>x.toUpperCase());return <DashboardLayout admin><h1>{name}</h1><p className="muted">Manage {section} from one secure administrative workspace.</p><section className="card" style={{marginTop:24}}><h3>{name} workspace</h3><p className="muted">This first milestone includes the dashboard structure and realistic preview data. Supabase authentication, role checks and database-backed actions are ready for the next integration phase.</p></section></DashboardLayout>}
