"use client";
import Link from "next/link";import { SiteHeader } from "@/components/site-header";import {useState} from "react";
export default function Checkout(){const [step,setStep]=useState(1);const labels=["Account","Registrant","Payment","Review","Confirmation"];if(step===5)return <><SiteHeader/><main className="page"><div className="shell" style={{maxWidth:720}}><section className="card" style={{textAlign:"center",padding:48}}><span className="icon" style={{margin:"auto",fontSize:22}}>✓</span><h1>You’re all set.</h1><p className="muted">Your order <strong>VNT-7K4M2P</strong> is being processed in test mode. We’ll email your confirmation shortly.</p><p className="muted">Domain registration · 1 year · $27.45</p><Link className="button" href="/dashboard">Go to dashboard</Link></section></div></main></> ;return <><SiteHeader/><main className="page"><div className="shell" style={{maxWidth:760}}><h1>Checkout</h1><div className="pills">{labels.map((x,i)=><span className="pill" key={x} style={i+1===step?{background:"#315efb",color:"#fff"}:{}}>{i+1}. {x}</span>)}</div><section className="card" style={{marginTop:24}}><h2>{labels[step-1]}</h2>{step===3?<><div className="notice">Test checkout is enabled until Stripe credentials are configured. No payment will be collected.</div><p className="muted">Card details will be securely collected by Stripe when live payments are enabled.</p></>:<><p className="muted">{step===1?"Create an account or sign in to continue.":step===2?"We use this information to register and manage your domain.":"Review your order before we process it."}</p><input aria-label="Full name" placeholder="Full name" style={{padding:12,border:"1px solid var(--line)",borderRadius:8,width:"100%"}}/></>}<div style={{display:"flex",justifyContent:"space-between",marginTop:28}}><button className="button ghost" disabled={step===1} onClick={()=>setStep(step-1)}>Back</button><button className="button" onClick={()=>setStep(step+1)}>{step===4?"Place test order":"Continue"}</button></div></section></div></main></>}
