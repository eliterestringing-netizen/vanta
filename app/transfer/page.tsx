import { SiteHeader } from "@/components/site-header";
export default async function Transfer({searchParams}:{searchParams:Promise<{domain?:string}>}){const {domain="yourdomain.com"}=await searchParams;return <><SiteHeader/><main className="page"><div className="shell" style={{maxWidth:700}}><h1>Transfer a domain</h1><p className="muted">Move {domain} to Vanta. First unlock it at your current registrar and request its authorisation code.</p><section className="card"><label>Domain<input defaultValue={domain} aria-label="Domain" style={{padding:12,border:"1px solid var(--line)",borderRadius:8,width:"100%",display:"block",marginTop:8}}/></label><label style={{display:"block",marginTop:16}}>Authorisation / EPP code<input aria-label="Authorisation code" style={{padding:12,border:"1px solid var(--line)",borderRadius:8,width:"100%",display:"block",marginTop:8}}/></label><button className="button" style={{marginTop:20}}>Start mock transfer</button></section></div></main></>}
