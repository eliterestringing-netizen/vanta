"use client";
import { useRouter } from "next/navigation"; import { useState } from "react";
export function DomainSearch({ initial = "" }:{initial?:string}){const [value,setValue]=useState(initial);const router=useRouter(); function search(e:React.FormEvent){e.preventDefault(); if(value.trim())router.push(`/domains?q=${encodeURIComponent(value.trim())}`)}return <><form onSubmit={search} className="searchbox" role="search"><input value={value} onChange={e=>setValue(e.target.value)} aria-label="Domain name" placeholder="mybusiness.com.au"/><button className="button" type="submit">Search</button></form><div className="pills" aria-label="Popular extensions">{[".com",".com.au",".au",".net",".org"].map(x=><button key={x} className="pill" onClick={()=>setValue(`mybusiness${x}`)}>{x}</button>)}</div></>}
