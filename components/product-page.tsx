import Link from "next/link";import { SiteHeader } from "./site-header";
export function ProductPage({eyebrow,title,description,plans}:{eyebrow:string;title:string;description:string;plans:{name:string;price:string;features:string[]}[]}){return <><SiteHeader/><main><section className="hero"><div className="shell"><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p className="lead">{description}</p></div></section><section className="section"><div className="shell"><div className="grid3">{plans.map((p,i)=><article className="card" key={p.name} style={i===1?{borderColor:"#315efb",boxShadow:"0 10px 30px #315efb18"}:{}}><span className="eyebrow">{p.name}</span><div className="metric" style={{marginTop:18}}>{p.price}</div><p className="muted">per month</p><ul className="muted" style={{paddingLeft:18,minHeight:135}}>{p.features.map(f=><li key={f} style={{marginBottom:9}}>{f}</li>)}</ul><Link href="/signup" className="button" style={{width:"100%"}}>Get started</Link></article>)}</div></div></section></main></>}
